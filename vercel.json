{
  "buildCommand": "if [ -d apps/web ]; then cd apps/web; fi; npx tsc -b && npx vite build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
